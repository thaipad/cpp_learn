strace ./res
трассировать программу "./res" 

gdb ./res
r
дебаггер программы

sudo ps aux | grep ./res | grep -v grep
посмотреть процессы "./res" (и не показывать при этом сам grep) 

lsof -p 9938
посмотреть инфу по конкретному процессу (по его PID)

strace -p 9938
начать трассировку процесса (можно в другом терминале подавать команды и смотреть, что процесс делает)

sudo ltrace -p 9938
посмотреть вызовы библиотек (тоже после запуска в другом терминале подавать команды)


